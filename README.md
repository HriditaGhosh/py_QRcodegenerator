# py-_QRcodegenerator
A feature-rich Python QR Code Generator with customizable colors, shapes, and scanning capabilities using Tkinter and qrcode libraries. Supports saving, scanning, and auto-opening URLs.
